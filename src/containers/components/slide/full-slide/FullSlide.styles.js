import styled from "styled-components";

export const TextInputWrapper = styled.div`
    position: absolute;
    top: 0;
    bottom: 0;
    width: inherit;
    max-height: inherit;
    padding: 1.5%;
`;
